## Packages
framer-motion | Page transitions and scroll-triggered animations
lucide-react | Standard icons for the application
react-hook-form | Form state management
@hookform/resolvers | Zod validation for forms
clsx | Class name merging
tailwind-merge | Tailwind class merging utility

## Notes
The app requires an API matching the provided @shared/routes schema.
API expects email to end with @thapar.edu for signup.
Authentication expects standard cookie-based sessions via /api/login and /api/register endpoints.
